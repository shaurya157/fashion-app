export default (theme) => ({
  root: {
    maxWidth: 345,
  },
  media: {
    height: 140,
  },
})

// root: {
//   padding: theme.spacing(2)
// },
// card: {
//   marginBottom: theme.spacing(2)
// }
